---
title: CLI
description: Opções e comandos da CLI do clawc.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

A CLI do clawc, por padrão, inicia o [TUI](/docs/tui) quando executada sem argumentos.

```bash
clawc
```

Mas também aceita comandos conforme documentado nesta página. Isso permite que você interaja com o clawc programaticamente.

```bash
clawc run "Explain how closures work in JavaScript"
```

---

### tui

Inicie a interface de usuário do terminal do clawc.

```bash
clawc [project]
```

#### Opções

| Flag                                     | Curto | Descrição                                                                  |
| ---------------------------------------- | ----- | -------------------------------------------------------------------------- |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continue a última sessão                                                   |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID da sessão para continuar                                                |
| <nobr><code>{"--fork"}</code></nobr>     |       | Criar um fork da sessão ao continuar (use com `--continue` ou `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>   |       | Prompt a ser usado                                                         |
| <nobr><code>{"--model"}</code></nobr>    | `-m`  | Modelo a ser usado na forma de provider/model                              |
| <nobr><code>{"--agent"}</code></nobr>    |       | Agente a ser usado                                                         |
| <nobr><code>{"--port"}</code></nobr>     |       | Porta para escutar                                                         |
| <nobr><code>{"--hostname"}</code></nobr> |       | Nome do host para escutar                                                  |

---

## Comandos

A CLI do clawc também possui os seguintes comandos.

---

### agent

Gerencie agentes para o clawc.

```bash
clawc agent [command]
```

---

### attach

Anexe um terminal a um servidor backend do clawc já em execução, iniciado via comandos `serve` ou `web`.

```bash
clawc attach [url]
```

Isso permite usar o TUI com um backend clawc remoto. Por exemplo:

```bash
# Inicie o servidor backend para acesso web/mobile
clawc web --port 4096 --hostname 0.0.0.0

# Em outro terminal, anexe o TUI ao backend em execução
clawc attach http://10.20.30.40:4096
```

#### Opções

| Flag                                     | Curto | Descrição                                                                         |
| ---------------------------------------- | ----- | --------------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |       | Diretório de trabalho para iniciar o TUI                                          |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continuar a última sessão                                                         |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID da sessão para continuar                                                       |
| <nobr><code>{"--fork"}</code></nobr>     |       | Bifurcar a sessão ao continuar (use com `--continue` ou `--session`)              |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Senha de autenticação básica (padrão: `CLAWC_SERVER_PASSWORD`)                 |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Usuário de autenticação básica (padrão: `CLAWC_SERVER_USERNAME` ou `clawc`) |

---

#### create

Crie um novo agente com configuração personalizada.

```bash
clawc agent create
```

Este comando irá guiá-lo na criação de um novo agente com um prompt de sistema personalizado e configuração de ferramentas.

---

#### list

Liste todos os agentes disponíveis.

```bash
clawc agent list
```

---

### auth

Comando para gerenciar credenciais e login para provedores.

```bash
clawc auth [command]
```

---

#### login

O clawc é alimentado pela lista de provedores em [Models.dev](https://models.dev), então você pode usar `clawc auth login` para configurar chaves de API para qualquer provedor que você gostaria de usar. Isso é armazenado em `~/.local/share/clawc/auth.json`.

```bash
clawc auth login
```

Quando o clawc é iniciado, ele carrega os provedores do arquivo de credenciais. E se houver chaves definidas em seus ambientes ou em um arquivo `.env` em seu projeto.

---

#### list

Lista todos os provedores autenticados conforme armazenado no arquivo de credenciais.

```bash
clawc auth list
```

Ou a versão curta.

```bash
clawc auth ls
```

---

#### logout

Desconecta você de um provedor limpando-o do arquivo de credenciais.

```bash
clawc auth logout
```

---

### github

Gerencie o agente do GitHub para automação de repositórios.

```bash
clawc github [command]
```

---

#### install

Instale o agente do GitHub em seu repositório.

```bash
clawc github install
```

Isso configura o fluxo de trabalho necessário do GitHub Actions e o guia pelo processo de configuração. [Saiba mais](/docs/github).

---

#### run

Execute o agente do GitHub. Isso é tipicamente usado em GitHub Actions.

```bash
clawc github run
```

##### Opções

| Flag                                  | Descrição                                        |
| ------------------------------------- | ------------------------------------------------ |
| <nobr><code>{"--event"}</code></nobr> | Evento simulado do GitHub para executar o agente |
| <nobr><code>{"--token"}</code></nobr> | Token de acesso pessoal do GitHub                |

---

### mcp

Gerencie servidores do Model Context Protocol.

```bash
clawc mcp [command]
```

---

#### add

Adicione um servidor MCP à sua configuração.

```bash
clawc mcp add
```

Este comando irá guiá-lo na adição de um servidor MCP local ou remoto.

---

#### list

Liste todos os servidores MCP configurados e seu status de conexão.

```bash
clawc mcp list
```

Ou use a versão curta.

```bash
clawc mcp ls
```

---

#### auth

Autentique-se com um servidor MCP habilitado para OAuth.

```bash
clawc mcp auth [name]
```

Se você não fornecer um nome de servidor, será solicitado que você selecione entre os servidores disponíveis habilitados para OAuth.

Você também pode listar servidores habilitados para OAuth e seu status de autenticação.

```bash
clawc mcp auth list
```

Ou use a versão curta.

```bash
clawc mcp auth ls
```

---

#### logout

Remova credenciais OAuth para um servidor MCP.

```bash
clawc mcp logout [name]
```

---

#### debug

Depure problemas de conexão OAuth para um servidor MCP.

```bash
clawc mcp debug <name>
```

---

### models

Liste todos os modelos disponíveis dos provedores configurados.

```bash
clawc models [provider]
```

Este comando exibe todos os modelos disponíveis entre seus provedores configurados no formato `provider/model`.

Isso é útil para descobrir o nome exato do modelo a ser usado em [sua configuração](/docs/config/).

Você pode opcionalmente passar um ID de provedor para filtrar modelos por esse provedor.

```bash
clawc models anthropic
```

#### Opções

| Flag                                    | Descrição                                                             |
| --------------------------------------- | --------------------------------------------------------------------- |
| <nobr><code>{"--refresh"}</code></nobr> | Atualiza o cache de modelos a partir do models.dev                    |
| <nobr><code>{"--verbose"}</code></nobr> | Use uma saída de modelo mais detalhada (inclui metadados como custos) |

Use a flag `--refresh` para atualizar a lista de modelos em cache. Isso é útil quando novos modelos foram adicionados a um provedor e você deseja vê-los no clawc.

```bash
clawc models --refresh
```

---

### run

Execute o clawc em modo não interativo passando um prompt diretamente.

```bash
clawc run [message..]
```

Isso é útil para scripts, automação ou quando você deseja uma resposta rápida sem iniciar o TUI completo. Por exemplo.

```bash "clawc run"
clawc run Explique o uso de context em Go
```

Você também pode se anexar a uma instância em execução do `clawc serve` para evitar tempos de inicialização a frio do servidor MCP em cada execução:

```bash
# Inicie um servidor sem cabeça em um terminal
clawc serve

# Em outro terminal, execute comandos que se anexam a ele
clawc run --attach http://localhost:4096 "Explique async/await em JavaScript"
```

#### Opções

| Flag                                     | Curto | Descrição                                                                         |
| ---------------------------------------- | ----- | --------------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |       | O comando a ser executado, use mensagem para argumentos                           |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continue a última sessão                                                          |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID da sessão para continuar                                                       |
| <nobr><code>{"--fork"}</code></nobr>     |       | Criar um fork da sessão ao continuar (use com `--continue` ou `--session`)        |
| <nobr><code>{"--share"}</code></nobr>    |       | Compartilhe a sessão                                                              |
| <nobr><code>{"--model"}</code></nobr>    | `-m`  | Modelo a ser usado na forma de provider/model                                     |
| <nobr><code>{"--agent"}</code></nobr>    |       | Agente a ser usado                                                                |
| <nobr><code>{"--file"}</code></nobr>     | `-f`  | Arquivo(s) a serem anexados à mensagem                                            |
| <nobr><code>{"--format"}</code></nobr>   |       | Formato: padrão (formatado) ou json (eventos JSON brutos)                         |
| <nobr><code>{"--title"}</code></nobr>    |       | Título para a sessão (usa o prompt truncado se nenhum valor for fornecido)        |
| <nobr><code>{"--attach"}</code></nobr>   |       | Anexe a um servidor clawc em execução (por exemplo, http://localhost:4096)     |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Senha de autenticação básica (padrão: `CLAWC_SERVER_PASSWORD`)                 |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Usuário de autenticação básica (padrão: `CLAWC_SERVER_USERNAME` ou `clawc`) |
| <nobr><code>{"--dir"}</code></nobr>      |       | Diretório de execução, ou caminho no servidor remoto ao anexar                    |
| <nobr><code>{"--variant"}</code></nobr>  |       | Variante do modelo (nível de raciocínio específico do provedor)                   |
| <nobr><code>{"--thinking"}</code></nobr> |       | Mostrar blocos de pensamento                                                      |
| <nobr><code>{"--port"}</code></nobr>     |       | Porta para o servidor local (padrão para porta aleatória)                         |

---

### serve

Inicie um servidor clawc sem cabeça para acesso à API. Confira a [documentação do servidor](/docs/server) para a interface HTTP completa.

```bash
clawc serve
```

Isso inicia um servidor HTTP que fornece acesso à funcionalidade do clawc sem a interface TUI. Defina `CLAWC_SERVER_PASSWORD` para habilitar a autenticação básica HTTP (o nome de usuário padrão é `clawc`).

#### Opções

| Flag                                     | Descrição                                             |
| ---------------------------------------- | ----------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Porta para escutar                                    |
| <nobr><code>{"--hostname"}</code></nobr> | Nome do host para escutar                             |
| <nobr><code>{"--mdns"}</code></nobr>     | Habilitar descoberta mDNS                             |
| <nobr><code>{"--cors"}</code></nobr>     | Origem(ns) de navegador adicionais para permitir CORS |

---

### session

Gerencie sessões do clawc.

```bash
clawc session [command]
```

---

#### list

Liste todas as sessões do clawc.

```bash
clawc session list
```

##### Opções

| Flag                                      | Curto | Descrição                                 |
| ----------------------------------------- | ----- | ----------------------------------------- |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`  | Limitar às N sessões mais recentes        |
| <nobr><code>{"--format"}</code></nobr>    |       | Formato de saída: tabela ou json (tabela) |

---

### stats

Mostre o uso de tokens e estatísticas de custo para suas sessões do clawc.

```bash
clawc stats
```

#### Opções

| Flag                                    | Descrição                                                                                            |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Mostre estatísticas dos últimos N dias (todo o tempo)                                                |
| <nobr><code>{"--tools"}</code></nobr>   | Número de ferramentas a serem mostradas (todas)                                                      |
| <nobr><code>{"--models"}</code></nobr>  | Mostre a divisão do uso de modelos (oculto por padrão). Passe um número para mostrar os N principais |
| <nobr><code>{"--project"}</code></nobr> | Filtrar por projeto (todos os projetos, string vazia: projeto atual)                                 |

---

### export

Exporte dados da sessão como JSON.

```bash
clawc export [sessionID]
```

Se você não fornecer um ID de sessão, será solicitado que você selecione entre as sessões disponíveis.

---

### import

Importe dados da sessão de um arquivo JSON ou URL de compartilhamento do clawc.

```bash
clawc import <file>
```

Você pode importar de um arquivo local ou de uma URL de compartilhamento do clawc.

```bash
clawc import session.json
clawc import https://clawc.ai/s/abc123
```

---

### web

Inicie um servidor clawc sem cabeça com uma interface web.

```bash
clawc web
```

Isso inicia um servidor HTTP e abre um navegador para acessar o clawc através de uma interface web. Defina `CLAWC_SERVER_PASSWORD` para habilitar a autenticação básica HTTP (o nome de usuário padrão é `clawc`).

#### Opções

| Flag                                     | Descrição                                             |
| ---------------------------------------- | ----------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Porta para escutar                                    |
| <nobr><code>{"--hostname"}</code></nobr> | Nome do host para escutar                             |
| <nobr><code>{"--mdns"}</code></nobr>     | Habilitar descoberta mDNS                             |
| <nobr><code>{"--cors"}</code></nobr>     | Origem(ns) de navegador adicionais para permitir CORS |

---

### acp

Inicie um servidor ACP (Agent Client Protocol).

```bash
clawc acp
```

Este comando inicia um servidor ACP que se comunica via stdin/stdout usando nd-JSON.

#### Opções

| Flag                                     | Descrição                 |
| ---------------------------------------- | ------------------------- |
| <nobr><code>{"--cwd"}</code></nobr>      | Diretório de trabalho     |
| <nobr><code>{"--port"}</code></nobr>     | Porta para escutar        |
| <nobr><code>{"--hostname"}</code></nobr> | Nome do host para escutar |

---

### uninstall

Desinstale o clawc e remova todos os arquivos relacionados.

```bash
clawc uninstall
```

#### Opções

| Flag                                        | Curto | Descrição                                |
| ------------------------------------------- | ----- | ---------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`  | Manter arquivos de configuração          |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`  | Manter dados de sessão e snapshots       |
| <nobr><code>{"--dry-run"}</code></nobr>     |       | Mostrar o que seria removido sem remover |
| <nobr><code>{"--force"}</code></nobr>       | `-f`  | Pular prompts de confirmação             |

---

### upgrade

Atualiza o clawc para a versão mais recente ou uma versão específica.

```bash
clawc upgrade [target]
```

Para atualizar para a versão mais recente.

```bash
clawc upgrade
```

Para atualizar para uma versão específica.

```bash
clawc upgrade v0.1.48
```

#### Opções

| Flag                                   | Curto | Descrição                                                        |
| -------------------------------------- | ----- | ---------------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`  | O método de instalação que foi usado; curl, npm, pnpm, bun, brew |

---

## Opções Globais

A CLI do clawc aceita as seguintes flags globais.

| Flag                                       | Curto | Descrição                               |
| ------------------------------------------ | ----- | --------------------------------------- |
| <nobr><code>{"--help"}</code></nobr>       | `-h`  | Exibir ajuda                            |
| <nobr><code>{"--version"}</code></nobr>    | `-v`  | Imprimir número da versão               |
| <nobr><code>{"--print-logs"}</code></nobr> |       | Imprimir logs no stderr                 |
| <nobr><code>{"--log-level"}</code></nobr>  |       | Nível de log (DEBUG, INFO, WARN, ERROR) |

---

## Variáveis de ambiente

O clawc pode ser configurado usando variáveis de ambiente.

| Variável                              | Tipo    | Descrição                                                             |
| ------------------------------------- | ------- | --------------------------------------------------------------------- |
| `CLAWC_AUTO_SHARE`                 | boolean | Compartilhar sessões automaticamente                                  |
| `CLAWC_GIT_BASH_PATH`              | string  | Caminho para o executável do Git Bash no Windows                      |
| `CLAWC_CONFIG`                     | string  | Caminho para o arquivo de configuração                                |
| `CLAWC_CONFIG_DIR`                 | string  | Caminho para o diretório de configuração                              |
| `CLAWC_CONFIG_CONTENT`             | string  | Conteúdo de configuração json inline                                  |
| `CLAWC_DISABLE_AUTOUPDATE`         | boolean | Desabilitar verificações de atualização automática                    |
| `CLAWC_DISABLE_PRUNE`              | boolean | Desabilitar a poda de dados antigos                                   |
| `CLAWC_DISABLE_TERMINAL_TITLE`     | boolean | Desabilitar atualizações automáticas do título do terminal            |
| `CLAWC_PERMISSION`                 | string  | Configuração de permissões json inline                                |
| `CLAWC_DISABLE_DEFAULT_PLUGINS`    | boolean | Desabilitar plugins padrão                                            |
| `CLAWC_DISABLE_LSP_DOWNLOAD`       | boolean | Desabilitar downloads automáticos do servidor LSP                     |
| `CLAWC_ENABLE_EXPERIMENTAL_MODELS` | boolean | Habilitar modelos experimentais                                       |
| `CLAWC_DISABLE_AUTOCOMPACT`        | boolean | Desabilitar compactação automática de contexto                        |
| `CLAWC_DISABLE_CLAUDE_CODE`        | boolean | Desabilitar leitura de `.claude` (prompt + habilidades)               |
| `CLAWC_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Desabilitar leitura de `~/.claude/CLAUDE.md`                          |
| `CLAWC_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Desabilitar carregamento de `.claude/skills`                          |
| `CLAWC_DISABLE_MODELS_FETCH`       | boolean | Desabilitar busca de modelos de fontes remotas                        |
| `CLAWC_FAKE_VCS`                   | string  | Provedor VCS falso para fins de teste                                 |
| `CLAWC_CLIENT`                     | string  | Identificador do cliente (padrão é `cli`)                             |
| `CLAWC_ENABLE_EXA`                 | boolean | Habilitar ferramentas de busca web Exa                                |
| `CLAWC_SERVER_PASSWORD`            | string  | Habilitar autenticação básica para `serve`/`web`                      |
| `CLAWC_SERVER_USERNAME`            | string  | Substituir nome de usuário de autenticação básica (padrão `clawc`) |
| `CLAWC_MODELS_URL`                 | string  | URL personalizada para buscar configuração de modelos                 |

---

### Experimental

Essas variáveis de ambiente habilitam recursos experimentais que podem mudar ou ser removidos.

| Variável                                        | Tipo    | Descrição                                                 |
| ----------------------------------------------- | ------- | --------------------------------------------------------- |
| `CLAWC_EXPERIMENTAL`                         | boolean | Habilitar recursos experimentais sob o sinalizador global |
| `CLAWC_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Habilitar descoberta de ícones                            |
| `CLAWC_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Desabilitar cópia ao selecionar no TUI                    |
| `CLAWC_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Tempo limite padrão para comandos bash em ms              |
| `CLAWC_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Máximo de tokens de saída para respostas LLM              |
| `CLAWC_EXPERIMENTAL_FILEWATCHER`             | boolean | Habilitar monitoramento de arquivos para todo o diretório |
| `CLAWC_EXPERIMENTAL_OXFMT`                   | boolean | Habilitar formatador oxfmt                                |
| `CLAWC_EXPERIMENTAL_LSP_TOOL`                | boolean | Habilitar ferramenta LSP experimental                     |
| `CLAWC_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Desabilitar monitoramento de arquivos                     |
| `CLAWC_EXPERIMENTAL_EXA`                     | boolean | Habilitar recursos experimentais do Exa                   |
| `CLAWC_EXPERIMENTAL_LSP_TY`                  | boolean | Habilitar TY LSP para arquivos python                     |
| `CLAWC_EXPERIMENTAL_PLAN_MODE`               | boolean | Habilitar modo de plano                                   |
| `CLAWC_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolean | Habilitar tarefas de subagentes em segundo plano          |
| `CLAWC_EXPERIMENTAL_EVENT_SYSTEM`            | boolean | Habilitar sistema de eventos experimental                 |
| `CLAWC_EXPERIMENTAL_NATIVE_LLM`              | boolean | Habilitar caminho nativo de requisição LLM                |
| `CLAWC_EXPERIMENTAL_PARALLEL`                | boolean | Habilitar execução paralela de busca web                  |
| `CLAWC_EXPERIMENTAL_SCOUT`                   | boolean | Habilitar subagente Scout                                 |
| `CLAWC_EXPERIMENTAL_WORKSPACES`              | boolean | Habilitar suporte a espaços de trabalho                   |
