name: publish
run-name: "${{ format('release {0}', inputs.bump) }}"

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      bump:
        description: "Bump major, minor, or patch"
        required: false
        type: choice
        options:
          - major
          - minor
          - patch
      version:
        description: "Override version (optional)"
        required: false
        type: string

concurrency: ${{ github.workflow }}-${{ github.ref }}-${{ inputs.version || inputs.bump }}

permissions:
  id-token: write
  contents: write

jobs:
  build-cli:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    if: github.repository == 'BlusceLabs/clawc'
    steps:
      - uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744 # v3.6.0
        with:
          fetch-tags: true

      - uses: ./.github/actions/setup-bun

      - name: Setup git committer
        id: committer
        uses: ./.github/actions/setup-git-committer
        with:
          clawc-app-id: ${{ vars.CLAWC_APP_ID }}
          clawc-app-secret: ${{ secrets.CLAWC_APP_SECRET }}

      - name: Build
        id: build
        run: |
          ./packages/clawc/script/build.ts
        env:
          CLAWC_VERSION: ${{ needs.version.outputs.version }}
          CLAWC_RELEASE: ${{ needs.version.outputs.release }}
          GH_REPO: ${{ github.repository }}
          GH_TOKEN: ${{ steps.committer.outputs.token }}

      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: clawc-cli
          path: |
            packages/clawc/dist/clawc-darwin*
            packages/clawc/dist/clawc-linux*

    outputs:
      version: ${{ needs.version.outputs.version }}
